// Write a JavaScript program to list the properties of a JavaScript object.

let student = {
    name: "Ivan Petrov",
    class: "X",
    number: 5
};

let output = "name: " + student.name + ", class: " + student.class + ", number: " + student.number;

console.log(output);
