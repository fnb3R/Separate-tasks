// Write a simple JavaScript program to join all elements of the following array into a string.

let seasons = ["Spring", "Summer", "Autumn", "Winter"];

let result1 = seasons.join(", ");
let result2 = seasons.join(" + ");

console.log(result1);
console.log(result2);